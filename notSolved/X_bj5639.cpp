#include <iostream>
#include <vector>
#include <queue>
#include <utility>

using namespace std;
queue<int> in_q;
queue<int> sub_q;
vector<int> preorder;
vector<pair<int,int> > v;

int main(){
    int num;
    while(cin >> num){
        in_q.push(num);
    }
    int tmp;
    while(in_q.size() >0){

    }
}