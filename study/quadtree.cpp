#include <iostream>
#include <cstdlib>
#include <string>

using namespace std; 
string input;
int len;

// int ReverseIndex(int left_start, int left_len, int right_start, int right_len){
//     string answer = "";
//     if(left_start != 0){
//         answer = answer + input.substr(0,left_start);
//     }
//     answer = answer + input.substr(left_start,left_len) 
//             + input.substr(left_start+left_len,right_start-1)
//             + input.substr(right_start,right_len);

//     if(right_start + right_len < len){
//         answer = answer + input.substr(right_start+right_len,len-right_start-right_len);
//     }

//     return 
// }

int main(){
    // string arr;
    // cin >> arr;
    // cout << arr << endl;
    // cout << arr.substr(0,3);
    cout << 9/2;
}